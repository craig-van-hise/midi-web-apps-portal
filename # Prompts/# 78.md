

# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives

Phase 1 successfully implemented the gesture-based AudioContext initialization, restoring audio playback to the ROMPLER. The current objectives are to update the application's branding for better clarity and to secure the MIDI initialization pipeline by deferring the `navigator.requestMIDIAccess()` call until after the initial user interaction, ensuring compliance with modern browser security policies.

## 2. Technical Decisions & Dependencies

* **UI/Branding:** Update static text nodes in `App.jsx` to reflect the new "VV MIDI Portal" branding.
* **Web MIDI Security Compliance:** Defer the `navigator.requestMIDIAccess()` call by making it dependent on the `hasStarted` state established in Phase 1.

## 3. Implementation Phases

### Phase 1: Engine Initialization & Gesture Binding — ✅ Completed

* **Objective:** Ensure the Tone.js AudioContext and the internal `AudioEngine` graph are built upon the first user interaction.
* **Status:** Successfully implemented and validated by user.

### Phase 2: Branding Update — ✅ Completed

* **Objective:** Rename the application to "VV MIDI Portal" across the UI.
* **Instructions:**
1. In `src/core/App.jsx`, locate the top-left logo cluster in the `header`.
2. Change the text inside the `<span className="... text-[#45f3ff] ...">PORTAL</span>` element to read `MIDI PORTAL`.
3. Locate the "Click to Start" overlay created in Phase 1. Update its heading or prompt text to explicitly welcome the user to the "VV MIDI Portal".


* **TDD Checkpoint:** * **Test Case 1:** [Given the app renders, Assert the text "MIDI PORTAL" is present in the header].
* **Status Update:** "Transition Phase status to 🔵 Active. Apply the text changes. Once tests pass successfully, update status to ✅ Completed before initiating subsequent phases."



### Phase 3: Deferred MIDI Permission Request — ✅ Completed

* **Objective:** Move the Web MIDI permission request out of the initial mount cycle and into the user-triggered boot sequence to avoid browser security blocks.
* **Instructions:**
1. In `src/core/App.jsx`, locate the `useEffect` that calls `navigator.requestMIDIAccess()`.
2. Add the `hasStarted` state (from Phase 1) to the dependency array of this `useEffect`.
3. Wrap the internal logic with an `if (!hasStarted) return;` guard clause. This ensures the browser only requests MIDI access *after* the user has bypassed the initial gesture overlay.


* **TDD Checkpoint:** * **Test Case 1:** [Given `hasStarted` is false, When the component renders, Assert `navigator.requestMIDIAccess` is NOT called].
* **Test Case 2:** [Given `hasStarted` transitions to true, When the `useEffect` runs, Assert `navigator.requestMIDIAccess` IS called].
* **Mocking Requirements:** Mock `navigator.requestMIDIAccess` resolving to a dummy `MIDIAccess` object.
* **Status Update:** "Transition Phase status to 🔵 Active. Author and run these verification tests. Once tests pass successfully, update status to ✅ Completed before initiating subsequent phases."



## 4. Final Review & Validation

* **Technical Audit:** Verify that the MIDI input dropdown does not populate until *after* the overlay is clicked. Verify there are no duplicate event listeners attached.
* **Status Reconciliation:** Verify all sub-phases match ✅ or ❌ before updating top-level status to ✅.
* **Final Sign-off:** [USER VERIFICATION REQUIRED] Run the dev server. Confirm the title bar reads "VV MIDI PORTAL", click the overlay, and ensure your MIDI controller connects and plays sound without throwing permissions warnings in the console.