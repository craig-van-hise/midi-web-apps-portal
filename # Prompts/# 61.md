# Product Requirements Prompt (PRP) — 🔵 Active

## 1. Project Context & Objectives
**Objective:** Restore missing ligature icons (Material Symbols) in the newly migrated `midi-tonnetz` plugin within the VV | WebApps Portal.
**Current State:** The Tonnetz plugin relies on Google Material Symbols Outlined for UI icons (e.g., `play_arrow`, `home`, `zoom_in`). Because the standalone Tonnetz app's `index.html` was bypassed during the plugin migration, the host portal currently lacks this font dependency, causing the icons to render as fallback text strings.
**Outcome:** The host portal's `index.html` will include the necessary Google Fonts stylesheet, globally restoring the intended UI iconography for the Tonnetz module without requiring a component rewrite.

## 2. Technical Decisions & Dependencies
* **Dependency:** Google Material Symbols Outlined.
* **Target File:** `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/index.html`
* **Implementation:** Inject a standard `<link>` tag into the document `<head>`.

## 3. Implementation Phases

### Phase 1: Inject Global Stylesheet — 🔵 Active
* **Objective:** Add the Material Symbols font to the portal's base HTML shell.
* **Instructions:**
    1. Open the portal's entry HTML file: `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/index.html`.
    2. Locate the `<head>` section.
    3. Inject the following stylesheet link element (preferably grouped with other font or preconnect links):
       `<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />`
    4. Save the file.
* **TDD Checkpoint:** * **Test Case 1:** [Given `index.html` is parsed, When searching the `<head>` tag, Assert the `fonts.googleapis.com` Material Symbols link exists].
    * **User Verification:** "[USER VERIFICATION REQUIRED] Ensure the local Vite development server is running. Open the 'VV | MIDI Tonnetz' module in the browser. Confirm that the text strings (e.g., 'play_arrow', 'home', 'zoom_in') have successfully rendered into their corresponding graphical icons."
    * **Status Update:** "Transition Phase status to 🔵 Active. Execute the HTML modification. Once complete and verified by the user, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Ensure the HTML tag is properly closed and does not introduce parsing errors in `index.html`.
* **Status Reconciliation:** Verify Phase 1 matches ✅ before updating top-level status to ✅.
* **Final Sign-off:** Present the finalized outcome to the user.