# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives

### 1.1 Summary of Objectives

Following a comprehensive debugging and refactoring session across the **VV | MIDI Chord Notator & Sequencer** suite (`src/plugins/chord-notator/`), we have resolved critical defects involving voice erasure, multi-voice "Katamari" unison catching, history stack corruption, infinite audio sustain, and step sequencer state desynchronization.

In an AI-native development environment (**Antigravity IDE 2.0**), coding agents naturally default to common JavaScript patterns—such as array deduplication (`new Set()`), greedy search (`.find()`), shallow object cloning (`{ ...obj }`), or naked audio triggers—unless explicitly constrained by persistent repository rules and automated verification.

To permanently protect our architectural fixes and guarantee **zero regression drift** when future AI agents implement new features, we must establish a **4-Pillar Regression Prevention Suite**:

1. **Antigravity IDE 2.0 Agent Rules (`.agent/rules/chord-notator-invariants.md`):** Codify an immutable "Never-Do" invariants sheet inside the repository's `.agent/` directory to instruct any AI coding agent on forbidden patterns and required architectural invariants.


2. **Regression Suite Consolidation & Tamper Protection:** Explicitly label our core edge-case test suites in `src/plugins/chord-notator/` as protected regression fixtures and establish a strict rule prohibiting agents from modifying, skipping (`test.skip`), or deleting existing tests without explicit human authorization.


3. **Automated NPM Verification Scripts (`package.json`):** Add a dedicated, fast-executing test script (`npm run test:notator`) to `package.json` to enable instant pre-flight regression audits and pre-commit verification.



### 1.2 Current Technical State & Target Architecture

* **Current `.agent/` Structure:** The repository currently utilizes an `.agent/` directory containing `.agent/workflows/import-midi-app.md`. Antigravity IDE 2.0 scans `.agent/rules/` and `.agent/workflows/` for repository-specific agent directives.


* **Current Test Suite:** The `src/plugins/chord-notator/` plugin suite contains 28+ test files and 225+ passing unit tests covering `NotationCanvas.tsx`, `StepSequencer.tsx`, `notationMath.ts`, and `chordSpeller.ts`.


* **Target Architecture:** We will create `.agent/rules/chord-notator-invariants.md`, register the new NPM test script in `package.json`, and audit existing test suites to ensure comment headers clearly mark regression invariants.



---

## 2. Technical Decisions & Dependencies

### 2.1 Antigravity IDE 2.0 Rule Directory Standard

* In Antigravity IDE 2.0, persistent agent guardrails reside in `.agent/rules/*.md`.
* Rules placed in `.agent/rules/` are automatically parsed by the AI agent before executing file modifications within matching directory paths.
* We will create `.agent/rules/chord-notator-invariants.md` with explicit **path scoping** (`paths: src/plugins/chord-notator/**`) and clear **Why/What** justifications so the agent understands the underlying musical and mathematical reasons for each constraint.

### 2.2 The 6 Immutable "Never-Do" Architectural Invariants

1. **Never Deduplicate or Sort Pitches During Transformation:** Chords must support polyphonic doublings (true unisons) and non-destructive voice crossing.
2. **Never Use Greedy Pitch Matching for Selection or Deletion:** Always select, target, and manipulate notes by their unique UUID (`note.id` / `data-id`). Never use `targetSelection.includes(note.note)` or `.find(n => n.note === pitch)`.
3. **Never Issue Naked `audioEngine.noteOn()` Calls for UI Previews:** All keyboard shortcuts, toolbar buttons, and interactive transformations must trigger audio previews exclusively through `playPreviewNotes(noteStrings, true, velocity)`.
4. **Never Use Shallow Stack Copies for Undo/Redo:** Every history push/pop in `commitState`, `undo`, `redo`, `commitSeqState`, `undoSeq`, and `redoSeq` must use strict deep cloning: `JSON.parse(JSON.stringify(...))`.
5. **Never Hide DOM Nodes Before Reading Geometry:** In selection marquees (`handlePointerUp`), always call `marqueeRef.current.getBoundingClientRect()` BEFORE adding the `'hidden'` CSS class.
6. **Never Hoist Upstream Broadcasts Above Local Ref Selection:** When handling canvas/sequencer pointer selection, always update local refs (`selectedStepRef.current = stepIndex`) BEFORE calling `updateActiveNotes(...)`.

---

## 3. Implementation Phases

### Phase 1: Create Antigravity IDE 2.0 Rule File (`.agent/rules/chord-notator-invariants.md`) — ✅ Completed

* **Objective:** Establish the permanent architectural guardrails file inside `.agent/rules/` so Antigravity IDE 2.0 coding agents automatically adhere to our non-negotiable invariants.
* **Instructions:**
1. Create a new directory `.agent/rules/` (if it does not already exist alongside `.agent/workflows/`).


2. Create a new file at `.agent/rules/chord-notator-invariants.md`.
3. Populate the file with the following exact markdown specification:
```markdown
---
description: "Immutable architectural invariants, regression prevention rules, and forbidden coding patterns for the VV | MIDI Chord Notator & Sequencer plugin suite."
globs: "src/plugins/chord-notator/**"
alwaysApply: false
---

# 🛡️ VV | Chord Notator — Immutable Architectural Invariants

Any AI developer or coding agent working within `src/plugins/chord-notator/` MUST strictly adhere to these non-negotiable architectural rules. Violating any of these rules will re-introduce critical regressions into polyphonic voice crossing, history traversal, audio sustain, or step sequencer synchronization.

## 1. The 6 "Never-Do" Coding Rules

### I. NEVER Deduplicate or Sort Pitches During Transformation
* **Forbidden:** `new Set(pitches)`, `.sort((a, b) => a - b)` inside `enforcePianoRange`, `handleMidiMessage` (`refresh: true`), or transformation helpers (`applyChromaticShift`, `applyDiatonicShift`, `applyPcsRotation`).
* **Required:** Preserve note arrays in their exact 1-to-1 index order.
* **Why:** Chords must support polyphonic doublings (true unisons) and non-destructive voice crossing. Sorting or deduplicating destroys array index alignment and permanently erases voices.

### II. NEVER Use Greedy Pitch Matching for Selection or Deletion
* **Forbidden:** `targetSelection.includes(note.note)`, `selectedNotes.includes(note.note)` for multi-voice toggling, or `.find(n => n.note === pitch)`.
* **Required:** Always select, highlight, target, and manipulate notes by their unique UUID (`note.id` / `data-id`). When matching external pitch arrays, enforce **1-to-1 cardinality matching** so each target pitch claims at most one unselected voice ID.
* **Why:** Matching by raw MIDI pitch number selects, moves, or erases all unison voices sharing that pitch simultaneously (the "Katamari" multi-voice catching bug).

### III. NEVER Issue Naked `audioEngine.noteOn()` Calls for UI Previews
* **Forbidden:** Calling `audioEngine.noteOn(noteStr, velocity)` directly inside UI click handlers, keyboard shortcuts, or transformation functions without an accompanying guaranteed timer.
* **Required:** Route all interactive UI audio previews through `playPreviewNotes(noteStrings, true, velocity)`.
* **Why:** Naked `noteOn` calls rely on key-up (`APP_TRANSFORM_OFF`) events to release notes. If key-up events are dropped or arrive out of order, voices sustain infinitely. `playPreviewNotes` enforces a strict 500ms self-terminating safety timer.

### IV. NEVER Use Shallow Stack Copies for Undo/Redo
* **Forbidden:** `undoStack.current.push(activeNotes.current.map(n => ({ ...n })))` or shallow array spreading `[...sequence]`.
* **Required:** Every history push and pop across `commitState`, `undo`, `redo`, `commitSeqState`, `undoSeq`, and `redoSeq` MUST use strict deep cloning: `JSON.parse(JSON.stringify(...))`.
* **Why:** Shallow copying allows nested object properties, accidental overrides, and spelling strings to mutate historical snapshots in place.

### V. NEVER Hide DOM Nodes Before Reading Geometry
* **Forbidden:** Adding the `'hidden'` CSS class (`marqueeRef.current.classList.add('hidden')`) before calling `.getBoundingClientRect()`.
* **Required:** Always evaluate `const rect = marqueeRef.current.getBoundingClientRect()` BEFORE applying `'hidden'`.
* **Why:** Adding `'hidden'` (`display: none`) instantly zeroes out the DOM bounding box (`width: 0, height: 0`) in JSDOM and browsers, causing marquee selection intersection tests to fail.

### VI. NEVER Hoist Upstream Broadcasts Above Local Ref Selection
* **Forbidden:** Calling `updateActiveNotes(...)` in `StepSequencer.tsx` before updating `selectedStepRef.current = startStep`.
* **Required:** Always set `setSelectedStep(startStep); selectedStepRef.current = startStep;` BEFORE calling `updateActiveNotes(...)`.
* **Why:** Calling `updateActiveNotes` synchronously broadcasts a `{ refresh: true }` event. If `selectedStepRef.current` still points to the previous bar index, the sequencer will overwrite the bar being left with the new bar's chord (the "Musical Chairs" bar overwrite bug).

---

## 2. Test Tampering & Regression Guardrail

* **Zero Test Tampering:** You are strictly forbidden from modifying, removing, or adding `.skip` / `test.skip` to any existing unit test in `src/plugins/chord-notator/` without explicit human authorization.
* **Regression Definition:** If your code modification causes any test in `npm run test:notator` to fail, your implementation is defective. You must refactor your implementation until all 225+ tests pass cleanly.

```




* **TDD Checkpoint:**
* **Test Case 1 (File Existence & Header Verification):**
* **Given:** The repository root in Antigravity IDE 2.0.
* **When:** Checking the filesystem at `.agent/rules/chord-notator-invariants.md`.
* **Then:** Assert the file exists and begins with valid frontmatter containing `globs: "src/plugins/chord-notator/**"`.


* **Test Case 2 (Content Audit):**
* **Input:** Read the contents of `.agent/rules/chord-notator-invariants.md`.
* **Output:** Assert the document contains all 6 Roman-numeral section headers (`I. NEVER Deduplicate...` through `VI. NEVER Hoist Upstream Broadcasts...`) and the `Zero Test Tampering` directive.


* **Status Update:** "Transition Phase status to 🔵 Active. Author and run these verification checks. Once verified successfully, update status to ✅ Completed before initiating subsequent phases."
* **User Verification:** `[USER VERIFICATION REQUIRED]` Pause for user review of the newly created `.agent/rules/chord-notator-invariants.md` file. Confirm that the rules accurately capture our debugging lessons and read clearly for future AI agents.



---

### Phase 2: Add NPM Verification Scripts (`package.json`) & Test Suite Audit — ✅ Completed

* **Objective:** Add a dedicated NPM script to `package.json` for instant execution of the Chord Notator test suite and audit core test files to ensure regression headers are documented.


* **Instructions:**
1. Open `package.json` and locate the `"scripts"` object.


2. Add the following script entry without modifying existing script commands:
```json
"test:notator": "vitest run src/plugins/chord-notator"

```


3. Open `src/plugins/chord-notator/components/NotationCanvas.test.tsx` and `src/plugins/chord-notator/components/StepSequencer.test.tsx`.


4. Ensure the top-level comment block of both files includes a clear notice linking to our invariants:
```typescript
/**
 * 🛡️ PROTECTED REGRESSION SUITE — DO NOT MODIFY OR SKIP
 * Verifies core invariants from .agent/rules/chord-notator-invariants.md including:
 * - 1-to-1 voice cardinality and non-destructive voice crossing
 * - Deep-cloned history stack traversal (Cmd+Z / Cmd+Shift+Z)
 * - 500ms self-terminating audio preview timers
 * - Marquee selection geometry and step sequencer synchronization
 */

```




* **TDD Checkpoint:**
* **Test Case 1 (Script Execution Verification):**
* **Given:** A terminal in the root directory of `midi-web-apps-portal`.


* **When:** Executing `npm run test:notator` from the command line.
* **Then:** Assert that Vitest executes specifically against `src/plugins/chord-notator/` and exits with return code `0` (all 28+ test files and 225+ tests passing).




* **Test Case 2 (Header Notice Verification):**
* **Input:** Inspect the first 15 lines of `NotationCanvas.test.tsx` and `StepSequencer.test.tsx`.


* **Output:** Assert the presence of the `PROTECTED REGRESSION SUITE` comment header linking to `.agent/rules/chord-notator-invariants.md`.


* **Status Update:** "Transition Phase status to 🔵 Active. Author and run these verification tests. Once tests pass successfully, update status to ✅ Completed before initiating subsequent phases."
* **User Verification:** `[USER VERIFICATION REQUIRED]` Pause for user review. Run `npm run test:notator` in your terminal and verify that the entire Chord Notator suite runs rapidly and passes 100% of its tests.



---

## 4. Final Review & Validation

* **Technical Audit:**
* Execute `npm run test:notator` to confirm zero regressions across all 225+ unit tests in `src/plugins/chord-notator/`.


* Confirm that `.agent/rules/chord-notator-invariants.md` is formatted with valid markdown frontmatter so Antigravity IDE 2.0 can parse its `globs` matching rules correctly.
* Verify that `package.json` remains valid JSON after adding the `"test:notator"` script entry.




* **Status Reconciliation:** Verify that Phase 1 and Phase 2 sub-phases transition from `🔘 Pending` -> `🔵 Active` -> `✅ Completed`. Ensure no phases remain in `🚨 Error` before updating top-level document status to `✅ Completed`.
* **Final Sign-off:** Present the completed Antigravity IDE 2.0 rules setup and test verification logs to the user for final sign-off.