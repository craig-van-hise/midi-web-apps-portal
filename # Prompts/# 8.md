
> **DO NOT VALIDATE YET. CLEF VERTICAL ALIGNMENT OVERSHOOT**
> **Context:** The previous adjustment overcompensated. The clefs moved up by *three* staff spaces instead of two. The treble clef curl is now floating above the G-line, and the bass clef dots are sitting above the top line.
> **The Objective:** Nudge the clefs back *down* by exactly one staff space so they lock into standard musical alignment.
> **Step 1: Check for Compounded Fixes**
> 1. Did you apply BOTH a CSS typography fix (like `line-height`) AND a manual `y` or `translateY` offset in the SVG/Canvas drawing logic?
> 2. If you applied both, they stacked and caused the overshoot. Remove the manual `y` offset in the drawing logic and rely solely on the CSS container fix (or vice versa).
> 
> 
> **Step 2: Fine-Tuning the Offset**
> 1. If you only adjusted a numerical `y` offset, dial the adjustment back. Since you moved it up 3 spaces instead of 2, reduce your upward adjustment delta by exactly one-third (e.g., if you subtracted 30px, change it to subtract 20px).
> 2. **Visual Verification Rule:** The center of the Treble Clef's bottom curl MUST wrap perfectly around the second line from the bottom (the G line). The two dots of the Bass Clef MUST perfectly straddle the second line from the top (the F line).
> 
> 
> **Execution:** Re-adjust the vertical offset down by one staff space. Run the local dev server, visually verify the alignment against the standard rule above, and ask me to check it again.