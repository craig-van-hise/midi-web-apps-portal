

> **DO NOT VALIDATE YET. ROMPLER UI FIXES & SIDEBAR UPGRADE**
> **Context:** The Rompler integration was successful functionally, but there are three distinct UI regressions/improvements we need to address before validation.
> **Issue 1: Missing LED Font**
> The green font for the MIDI Channel ("01") and Instrument Patch ("VIBRAPHONE") does not match the original hardware style.
> * **Fix:** Go back to the remote `midi-chord-notator-web` repository (or inspect the local `public` folder). Find the specific font file used for these LED displays. Ensure the font file is copied into the portal's `public/fonts/` directory. Check `src/core/rompler/rompler.css` and fix the `@font-face` `src(url(...))` path so the custom font loads correctly.
> 
> 
> **Issue 2: Drawer Layout Overlap**
> The Rompler drawer is currently taking up the entire bottom width of the screen, obscuring the Left Sidebar. It should only occupy the space at the bottom of the *Main Application Pane*.
> * **Fix:** Move the `<MasterRompler />` component in the DOM tree. It should NOT be a sibling to the Sidebar. It MUST be a child of the right-hand container.
> * **DOM Structure Expectation:**
> `<AppRoot flex>`
> `<Sidebar />`
> `<MainColumn flex-col flex-1>`
> `<TopBar />`
> `<ActivePluginViewer flex-1 />`
> `<MasterRompler />`  <-- Move it here!
> `</MainColumn>`
> `</AppRoot>`
> 
> 
> **Issue 3: Sidebar UX Improvements**
> * **Rename:** Change the text "APPLICATION REGISTRY" to "MIDI MODULES".
> * **Collapsible State:** Make the Left Sidebar collapsible. Introduce a state variable (e.g., `isSidebarOpen`).
> * **Toggle Button:** Add a small Lucide icon button (like `ChevronLeft` / `ChevronRight` or `Menu`) in the sidebar header to toggle this state.
> * **Animation:** When collapsed, the sidebar width should smoothly transition to a minimal width (e.g., just enough to show the app icons, or completely hidden), allowing the MainColumn to expand and give the active plugin more breathing room.
> 
> 
> **Execution:** Fix the font import, correct the Rompler's DOM placement so it respects the sidebar's space, and implement the collapsible sidebar logic. Run the dev server and report back for visual confirmation.