

**STOP. You broke the application.** The Rompler is now outputting dead air, and your human validation step completely ignored testing the actual audio latency.

Execute the following fixes immediately:

1. **Fix the Audio Engine (Autoplay Policy):** Do NOT initialize `new Tone.Context(...)` at the top-level of `engine.js`. Browsers block audio contexts created without a user gesture. You must move the `Tone.setContext(new Tone.Context({ latencyHint: "interactive" }))` call **INSIDE** the user-triggered initialization routine (e.g., the `init` or `start` function triggered by the portal's Power button), immediately before `Tone.start()`.
2. **Rewrite the Validation Step:** Change the Phase 1 Human Validation step. It must explicitly instruct me to: *"Turn on the portal, play a chord on the connected MIDI controller, and verify that the audio actually plays and that the initial latency feels perceptibly tighter."*
3. **Reset Status:** Revert Phase 1 back to 🔵 **Active**. Do not mark it completed until I confirm the audio engine is actually generating sound again.