

**PROJECT DIRECTIVE: Porting Plugin #1 (Chord Notator)**

**Context:** The Plugin Host architecture is verified. The `DummyPlugin` successfully receives props and communicates with the Portal's Rompler. We will now port the first actual application (`midi-chord-notator-web`) into the Portal.

**Environment Map:**
You are in `midi-web-apps-portal`. The target repository to ingest is `../midi-chord-notator-web`.

**Objective:**
Migrate the source code, reconcile dependencies, strip out the standalone UI/audio, and wire it to the new prop-based interface.

**Step 1: Dependency Reconciliation**

1. Read the `package.json` of `../midi-chord-notator-web`.
2. Identify any core dependencies required for the app to function (e.g., sheet music rendering libraries, specific math utilities, etc.). Ignore standard React/Vite/Tailwind dev dependencies.
3. Install those specific dependencies into the `midi-web-apps-portal` repository using `npm install`.

**Step 2: Tailwind Configuration Update**
Ensure that the Tailwind configuration in `midi-web-apps-portal` is explicitly set to scan `src/plugins//*.{js,jsx,ts,tsx}` so the injected component styles render correctly.

**Step 3: Code Ingestion & Strict Siloing**

1. Create the directory `src/plugins/chord-notator`.
2. Copy the relevant source files (components, utils, hooks) from `../midi-chord-notator-web/src` into `src/plugins/chord-notator`.
3. Do NOT copy the old `App.jsx`, `main.jsx`, or standalone entry points.

**Step 4: The "Headless" Refactor**
Create `src/plugins/chord-notator/index.jsx` as the single default export. This component MUST implement the following:

1. **Accept Standardized Props:** `({ midiIn, onMidiOut, isBypassed, showInfo, showSettings, triggerPanic })`
2. **Remove Native Implementations:** * Delete any internal `<Header>`, Title Bars, Logos, or UI buttons.
* Delete `<MidiInputSelector>` and `<MidiOutputSelector>`.
* Delete all `navigator.requestMIDIAccess()` logic.
* Delete the local Tone.js Rompler drawer (the Portal handles audio now).


3. **Wire the Logic:**
* Use a `useEffect` to listen to the `midiIn` prop (which will contain raw hardware MIDI data like `[144, 60, 100]`) and pipe it directly into the notation processing engine.
* If the app generates any preview sounds, replace them with `onMidiOut(midiData)`.
* Bind the `showInfo` and `showSettings` boolean props to the visibility states of the app's native modals.
* Bind the `triggerPanic` prop to clear the visual notation sheet.



**Step 5: Portal Integration**

1. Import `ChordNotator` from `src/plugins/chord-notator` into the Portal's main dynamic renderer.
2. Update the `appRegistry` (or component map) so that selecting "VV | MIDI Chord Notator" in the sidebar mounts this actual component instead of the `DummyPlugin`.
3. Run `npx vitest run` and fix any import or component binding errors.

**HARD STOP: Human Validation Checkpoint**
Once the Chord Notator is integrated and the app compiles successfully, halt execution. Output the following exact text to the user:

"**PLUGIN #1 INTEGRATED. READY FOR HUMAN VALIDATION.** Please run `npm run dev` and verify the following:

1. Select the 'Chord Notator' from the left sidebar.
2. Verify the sheet music UI renders cleanly without its old Title Bar or MIDI dropdowns.
3. Select a MIDI Input from the *Portal's* top bar and play a chord. Verify the sheet music updates.
4. Verify the audio plays correctly through the Portal's bottom Rompler.
5. Click the 'i' and 'Cog' buttons on the Portal top bar and ensure the Chord Notator's specific modals open.

If everything works perfectly, reply **'VALIDATED'** and we will proceed to porting the remaining plugins."