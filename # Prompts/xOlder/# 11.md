

> **DO NOT VALIDATE YET. MATRIX SIZING & ALIGNMENT BUG**
> **Context:** The CSS isolation worked perfectly for the styles/colors, but now we have a severe sizing and layout bug. The Matrix grid is rendering massively oversized. Because it is stretching, the grid cells no longer align with the input/output labels (C, C#, D), and the bloat is pushing the keyboard component completely off the bottom of the screen.
> **The Problem:** The Portal's main container is likely forcing the plugin's root wrapper to stretch to 100% width/height, causing the SVG or CSS Grid of the Matrix to expand beyond its intended dimensions.
> **The Objective:** Constrain the dimensions of the Matrix component to match the original repo, ensuring label alignment and bringing the keyboard back into view.
> **Step 1: Constrain the Main Wrapper**
> 1. Look at the original `../midi-pitch-class-matrix-web/src/App.jsx`. How was the main content area constrained? Did it have a `max-w-5xl`, `max-w-fit`, or a specific fixed pixel width?
> 2. Apply that exact width constraint to the root wrapper inside `src/plugins/pitch-class-matrix/index.jsx`. Use `mx-auto` to keep it centered.
> 
> 
> **Step 2: Prevent Flexbox Stretching**
> 1. Inspect the flex container that holds the three columns: `[Presets Card] | [Matrix] | [Scale Root Card]`.
> 2. Ensure this container has `items-start` or `items-center` applied to it. If it defaults to `stretch` (or if the children have `flex-1` without bounds), it will cause the middle Matrix to bloat.
> 
> 
> **Step 3: Fix Grid vs. Label Alignment**
> 1. Look at the actual `<Matrix>` component code. The grid cells and the axis labels must share the same sizing logic.
> 2. If the grid is an SVG, make sure its `width` and `height` attributes match the original exactly (do not let them default to "100%").
> 3. If it is a CSS Grid, ensure the column/row sizes (e.g., `w-8 h-8` or `2rem`) are strictly enforced and not being overridden.
> 
> 
> **Execution:** > 1. Apply the max-width wrappers.
> 2. Lock the Matrix cell sizes so they perfectly align with the C, C#, D labels again.
> 3. Verify that shrinking the matrix pulls the piano keyboard back up into full view without clipping.
> 4. Report back when fixed for visual confirmation.