# Product Requirements Prompt (PRP) — 🔵 Active

## 1. Project Context & Objectives
**Context:** In the `chord-notator` plugin, the transformations toolbar/drawer located beneath the keyboard is experiencing two UI/UX issues: 1) It currently defaults to a closed state, and 2) When closed, the chevron toggle button is partially obscured by the keyboard above it.
**Objective:** Update the component's state initialization so the drawer defaults to open upon load. Adjust the CSS or Framer Motion variant values to ensure the toggle tab is fully visible and accessible when the drawer is in the closed position.

## 2. Technical Decisions & Dependencies
* **State Management:** The boolean state controlling the drawer's visibility (likely located in `Keyboard.tsx`, `NotationCanvas.tsx`, or a dedicated file in `components/toolbar/`) must be initialized to `true`.
* **Styling / Animation:** The `midi-web-apps-portal` utilizes Tailwind CSS and Framer Motion (`motion`). The developer must identify the closed state positioning (e.g., `translateY` offset, bottom margin, or absolute positioning `bottom`/`top` values) and increase the offset so the chevron tab clears the keyboard's bounding box entirely.

## 3. Implementation Phases

### Phase 1: Default State Adjustment — ✅ Completed
* **Objective:** Ensure the transformations drawer defaults to an open state on initial render.
* **Instructions:**
  1. Locate the component containing the transformations drawer (check `src/plugins/chord-notator/components/toolbar/` or `Keyboard.tsx`).
  2. Find the React `useState` hook (e.g., `const [isOpen, setIsOpen] = useState(false)`) that controls the drawer's visibility.
  3. Change the initial value from `false` to `true`.
* **TDD Checkpoint:**
  * **Test Case 1:** *Given* the plugin mounts, *Then* the drawer state should evaluate to true, and the drawer should be rendered in its expanded view without requiring user interaction.
  * **Status Update:** "Transition Phase status to 🔵 Active. Implement the state change. Verify via unit tests if applicable, or local testing. Once confirmed, update status to ✅ Completed."

### Phase 2: Closed State Positioning (Chevron Visibility) — ✅ Completed
* **Objective:** Prevent the keyboard from obscuring the drawer's toggle tab when closed.
* **Instructions:**
  1. Inspect the CSS classes, inline styles, or Framer Motion `variants` controlling the closed/collapsed state of the drawer.
  2. Calculate the height of the toggle tab (the element containing the chevron `^`).
  3. Adjust the closed position (e.g., changing a `translateY` value from `100%` to `calc(100% - 2rem)` or adjusting absolute padding/margins) so the entire toggle tab remains visible below the keyboard.
  4. Ensure z-index layers are appropriate (the drawer tab should not sit behind the keyboard wrapper).
* **TDD Checkpoint:**
  * **Test Case 1:** *Given* the drawer is toggled to the closed state, *Then* the calculated Y-position must leave the chevron button's height fully exposed.
  * **Status Update:** "Transition Phase status to 🔵 Active. Apply the styling adjustments. Once applied, update status to ✅ Completed."

### Phase 3: Visual Validation — 🔵 Active
* **Objective:** Ensure the layout fixes look correct in the browser environment.
* **Instructions:** Start the Vite dev server and manually verify the changes.
* **TDD Checkpoint:**
  * **User Verification (Required):** "[USER VERIFICATION REQUIRED] Pause for user review. Please run `npm run dev`. Confirm that: 1) The drawer is fully open when you first load the plugin. 2) When you click the chevron to close it, the tab and chevron remain fully visible beneath the keyboard and are not cut off. Reply 'Approved' when confirmed."
  * **Status Update:** "Transition Phase status to 🔵 Active. Await user confirmation. Once approved, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Ensure the CSS/Framer Motion changes do not cause unwanted scrollbars or layout shifts on smaller window sizes.
* **Status Reconciliation:** Verify all sub-phases match ✅ Completed before updating the top-level status to ✅ Completed.
* **Final Sign-off:** Present the exact files modified to the user for final project closure.