
# 🚨 CRITICAL FIX: Resolve Native to Tone.js Node Connection — ✅ Completed

## 1. Root Cause Analysis
The Worklet initializes successfully, but the application throws `parameter 1 is not of type 'AudioNode'` at the connection step. This occurs because `this.internalTrim` is a Tone.js `Tone.Gain` object, not a native Web Audio `AudioNode`. The browser's native `AudioWorkletNode.connect()` strictly rejects Tone wrappers. We must drill down into `this.internalTrim` to extract the underlying native `GainNode` and establish the connection natively.

## 2. Implementation Instructions

### Phase 1: Native Node Extraction & Connection — ✅ Completed
* **Objective:** Replace Tone's brittle connection patch with a bulletproof native node resolver.
* **Instructions:**
  1. Open `src/core/rompler/engine.js`.
  2. Locate the `AudioWorklet Bridge` try/catch block inside `init(sabBuffer)`.
  3. Find the line that attempts to connect the Worklet (e.g., `Tone.connect(this.nativeWorklet, this.internalTrim)` or `this.nativeWorklet.connect(...)`).
  4. Replace that connection line with the following precise drill-down logic:
  ```javascript
      // GUARANTEED to succeed because this.trueNativeCtx is undeniably BaseAudioContext
      this.nativeWorklet = new AudioWorkletNode(this.trueNativeCtx, 'rompler-processor');
      
      // Extract the raw native AudioNode from the Tone.js wrapper
      let targetDest = this.internalTrim;
      while (targetDest && !(targetDest instanceof AudioNode)) {
        let next = targetDest.input || targetDest._gainNode;
        if (!next || next === targetDest) break;
        targetDest = next;
      }
      
      // Connect the native Worklet straight to the extracted native GainNode
      if (targetDest && targetDest instanceof AudioNode) {
        this.nativeWorklet.connect(targetDest);
      } else {
        console.warn("[AudioEngine] Could not resolve native node for internalTrim, connecting to destination as fallback.");
        this.nativeWorklet.connect(this.trueNativeCtx.destination);
      }

      if (this.sabBuffer) {
        this.nativeWorklet.port.postMessage({ type: 'SAB', buffer: this.sabBuffer });
      }
      console.log('[AudioEngine] AudioWorklet bridge established successfully.');

```

* **AGENT INSTRUCTION:** Make this specific replacement. Run Vitest `npm run test run` to ensure tests remain green. Set status to 🔵 Active, then ✅ Completed.

## 3. Final Verification

* **HUMAN VALIDATION REQUIRED:** Stop and ask the user to refresh the page, turn on the Rompler, and play a chord. The audio should now flow seamlessly from the Worklet through the Tone.js mixer out to the speakers with zero main-thread GC latency.

```

