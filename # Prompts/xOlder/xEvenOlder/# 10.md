

> **DO NOT VALIDATE YET. MAJOR UI REGRESSION ON PLUGIN: MIDI Pitch Class Matrix**
> **Context:** I have reviewed the local dev server and there are major visual regressions in the "MIDI Pitch Class Matrix" plugin (`src/plugins/pitch-class-matrix`). The "result" (`Screenshot 2026-05-24 at 6.56.14 PM.png`) is completely marred and does not match the original design (`Screenshot 2026-05-23 at 3.19.32 PM.jpg`). We must prioritize a faithful visual reproduction of the original without manual tweaking.
> **The Problem:** The previous migration likely failed because it tried to adapt the plugin's native styles, or allowed global Tailwind CSS styles to leak in and conflict with the un-adapted original styles.
> **The Objective:** Abandon all adaptation. Use the "Strict CSS Isolation" method to force the plugin's original, un-modified CSS to have total control over the visual rendering.
> **Execution Plan: Strict CSS Isolation & DOM Restoration**
> Execute these steps precisely and sequentially for the `src/plugins/pitch-class-matrix` directory.
> **Step 1: CSS Isolation (The Firewall)**
> 1. Create a dedicated CSS file *only* for this plugin (e.g., `src/plugins/pitch-class-matrix/styles/matrix.css`).
> 2. Copy the entire un-adapted, original CSS codebase for the plugin into this file.
> 3. Target *all* styles in this file using a wrapper class (e.g., `.headless-matrix-plugin`).
> 4. In `src/plugins/pitch-class-matrix/index.jsx`, wrap the *entire* main layout `<div>` in this specific `.headless-matrix-plugin` class to guarantee styles do not leak or conflict.
> 
> 
> **Step 2: DOM Hierarchy & Layout Restoration**
> 1. In `src/plugins/pitch-class-matrix/index.jsx`, ruthlessly restore the exact DOM hierarchy from the original repo.
> 2. **Correct "Output Note" Positioning:** In the result, the "Output Note" card is right next to the "Scale Root" card. In the original, it is on the far right of the matrix columns. Move the `<OutputNoteCard>` to its original position in the JSX, *after* the matrix column containers.
> 3. **Correct Keyboard Position:** The keyboard is clipping at the bottom, and there is too much negative space between the matrix and the keys. Ensure the keyboard component sits close to the bottom of the matrix container, mirroring the spacing in the original screenshot. Remove any large margins or padding causing this space.
> 
> 
> **Step 3: Verification Audit against Specific Bugs**
> * **Background Color:** Result is brownish; original is light grey. Verify.
> * **typography (Labels):** Labels like "presets," "C major scale," "scale Roots" are light grey and underlined in the result. Original has them as strong, dark text. Verify.
> * **Matrix Lines & Shades:** Lines are different weights, and cell grey shades are wrong. The original uses a specific lighter palette for black keys. Verify.
> 
> 
> **Execution:** Establish the isolated CSS firewall, restore the exact DOM hierarchy and card positions, audit the results against the specific bugs listed, and ask me to visually check it again.