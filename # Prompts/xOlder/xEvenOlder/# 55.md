# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
We need to extract the legacy Tone.js audio engine file from a previous git commit to analyze its volume normalizations, sample maps, and node routing. This file must be safely extracted to the project root for reference *without* overwriting the current active native web audio engine.

## 2. Implementation Phases

### Phase 1: Retrieve Legacy Engine File — ✅ Completed
* **Objective:** Extract the legacy `engine.js` from the commit containing the message "changes to transposer UI".
* **Instructions:**
  1. Locate the exact commit hash for the commit message matching "changes to transposer UI".
     *(e.g., `git log --grep="changes to transposer UI" -i --format="%H" -n 1`)*
  2. Use `git show` to extract the contents of `src/core/rompler/engine.js` from that specific commit.
  3. Output the contents into a new file in the project root directory named `engine_legacy_reference.js`.
  4. **CRITICAL:** Do NOT modify, checkout, or replace the current working tree's `src/core/rompler/engine.js`.
* **TDD Checkpoint:**
  * **Test Case 1:** Verify the file `engine_legacy_reference.js` has been created in the root directory.
  * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute the terminal commands to retrieve the file. Once the file is confirmed in the root directory, set status to ✅ Completed. Do not proceed until status is ✅."

## 3. Final Review & Validation
* **Status Reconciliation:** Verify Phase 1 is ✅ Completed before marking the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** "STOP. Inform the user that `engine_legacy_reference.js` has been successfully placed in the project root and is ready for them to review and hand off."