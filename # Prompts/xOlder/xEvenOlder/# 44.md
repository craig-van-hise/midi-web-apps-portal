
# 🚨 CRITICAL FIX: Bypass Tone.js Prototype Poisoning — ✅ Completed

## 1. Root Cause Analysis
The Worklet initialized perfectly, but the connection failed because Tone.js overrides the global `window.AudioNode` class. Because of this, the previous `instanceof AudioNode` safety check evaluated to `true` for a Tone.js wrapper, bypassed the extraction loop, and fed a Javascript object into the strict native C++ `.connect()` method. We must forcefully drill down to the native node and ignore Tone's prototype chain entirely.

## 2. Implementation Instructions

### Phase 1: Unconditional Native Node Extraction — ✅ Completed
* **Objective:** Forcefully extract the underlying native Web Audio node from the Tone object by drilling through its internal properties until we hit the bottom.
* **Instructions:**
  1. Open `src/core/rompler/engine.js`.
  2. Locate the Worklet initialization block inside `init(sabBuffer)`.
  3. Replace the connection logic with this exact, uncompromising drill-down:
  ```javascript
      // GUARANTEED to succeed because this.trueNativeCtx is undeniably BaseAudioContext
      this.nativeWorklet = new AudioWorkletNode(this.trueNativeCtx, 'rompler-processor');
      
      // Force extraction of the pure native GainNode. 
      // Do NOT use instanceof or Tone.connect, as Tone poisons the prototype chain.
      let pureNativeNode = this.internalTrim;
      while (pureNativeNode && (pureNativeNode.input || pureNativeNode._gainNode)) {
        let next = pureNativeNode.input || pureNativeNode._gainNode;
        if (next === pureNativeNode) break;
        pureNativeNode = next;
      }
      
      // pureNativeNode is now 100% guaranteed to be the C++ Web Audio Node
      this.nativeWorklet.connect(pureNativeNode);

      if (this.sabBuffer) {
        this.nativeWorklet.port.postMessage({ type: 'SAB', buffer: this.sabBuffer });
      }
      console.log('[AudioEngine] AudioWorklet bridge established successfully.');

```

* **AGENT INSTRUCTION:** Make this specific replacement. Run Vitest `npm run test run` to ensure tests remain green. Set status to 🔵 Active, then ✅ Completed.

## 3. Final Verification

* **HUMAN VALIDATION REQUIRED:** Stop and ask the user to refresh the page, turn on the Rompler, and verify the audio pipeline.

```

