# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
The keyswitch illumination is failing immediately after the initial press because a downstream state update (`selectedNotes`) triggers a `useEffect` in `Piano88`, which blindly sends an empty color string (`''`) to the keyswitch, turning it off prematurely. The objective is to make `updateKeyVisuals88` self-aware: it must internally query the module-level `activeKeyswitchesTracker`. If a keyswitch is physically held, the function must forcefully ignore any external commands attempting to turn it off.

## 2. Technical Decisions & Dependencies
* **Function-Level Armor:** `updateKeyVisuals88` will read `activeKeyswitchesTracker.has(note)` directly. If true, it immutably forces `finalColor` to the keyswitch's semantic color.
* **Tracker Validation:** Ensure the tracker accurately intercepts the `midiNote` from the enriched `APP_BUTTON_PRESS` payload implemented in the previous phase.

## 3. Implementation Phases

### Phase 11: Active Keyswitch Fortress — ✅ Completed
* **Objective:** Armor the DOM manipulator to reject rogue React lifecycle updates.
* **Instructions:**
    1. **Verify the Module Tracker:** Ensure this code exists exactly like this at the very top of `Keyboard.tsx` (outside of the component):
       ```typescript
       // Absolute hardware tracking for keyswitches
       const activeKeyswitchesTracker = new Set<number>();
       if (typeof window !== 'undefined') {
           window.addEventListener('APP_BUTTON_PRESS_ON', (e: any) => {
               const note = e.detail?.midiNote;
               if (note !== undefined) {
                   activeKeyswitchesTracker.add(note);
                   updateKeyVisuals88(note, 'ks_active');
               }
           });
           window.addEventListener('APP_BUTTON_PRESS_OFF', (e: any) => {
               const note = e.detail?.midiNote;
               if (note !== undefined) {
                   activeKeyswitchesTracker.delete(note);
                   updateKeyVisuals88(note, '');
               }
           });
       }
       ```
    
    2. **Replace `updateKeyVisuals88`:** Scroll to the bottom of `Keyboard.tsx` and completely replace the `updateKeyVisuals88` function with this armored version:
       ```typescript
       export const updateKeyVisuals88 = (note: number, color: string) => {
           const el = document.getElementById(`pk88-${note}`);
           if (!el) return;

           const isBlack = [1, 3, 6, 8, 10].includes(note % 12);
           const ksColor = el.dataset.ksColor;
           
           // The function checks the hardware truth itself
           const isKsHeld = activeKeyswitchesTracker.has(note); 

           let finalColor = color;
           
           // ARMOR: If it's a keyswitch and it's physically held, FORCE IT ON.
           // This prevents rogue React effects from turning it off with a '' command.
           if (ksColor && isKsHeld) {
               finalColor = ksColor; 
           } else if (ksColor && color && color !== '') {
               finalColor = ksColor; // Allow standard UI clicks to trigger it
           }

           if (finalColor && finalColor !== '') {
               // ACTIVE STATE
               el.style.backgroundColor = finalColor;
               el.style.boxShadow = `inset 0 -5px 10px rgba(0,0,0,0.1), 0 0 12px ${finalColor}`;
               if (isBlack) {
                   el.style.zIndex = '11';
                   el.style.borderBottom = '1px solid #000';
                   el.style.borderLeft = '1px solid #333';
                   el.style.borderRight = '1px solid #333';
                   if (ksColor) el.style.borderTop = `2px solid ${ksColor}`;
               }
           } else {
               // IDLE STATE
               if (ksColor) {
                   el.style.backgroundColor = isBlack 
                       ? `color-mix(in srgb, ${ksColor} 60%, #444444)` 
                       : `color-mix(in srgb, ${ksColor} 45%, #ffffff)`;
                   el.style.borderTop = `2px solid ${ksColor}`;
               } else {
                   el.style.backgroundColor = isBlack ? '#444' : '#fff';
                   el.style.borderTop = 'none';
               }
               el.style.boxShadow = 'none';
               if (isBlack) {
                   el.style.zIndex = '10';
                   el.style.borderBottom = '6px solid #000';
                   el.style.borderLeft = '1px solid #333';
                   el.style.borderRight = '1px solid #333';
               }
           }
       };
       ```

* **TDD Checkpoint:** * **Test Case 1:** [Given a keyswitch is physically held, When the React `useEffect` loop calls `updateKeyVisuals88(note, '')`, Assert the function overrides the empty string with `ksColor` and keeps the key illuminated].
    * **Status Update:** "Transition Phase status to 🔵 Active. Implement the armored function replacement. Once verified, update status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Ensure the `activeKeyswitchesTracker` is defined in the same file and scope so `updateKeyVisuals88` can access it without throwing a reference error.
* **Status Reconciliation:** Verify Phase 11 matches ✅ before updating top-level status to ✅.
* **Final Sign-off:** [USER VALIDATION REQUIRED] Rapid-fire press a mapped keyswitch. Confirm it flawlessly lights up 100% of the time and ignores the sheet music's visualizer updates.