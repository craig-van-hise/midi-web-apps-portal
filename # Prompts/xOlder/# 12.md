

**PROJECT DIRECTIVE: Porting the Master Rompler (Tone.js Audio Engine)**

**Context Primer (Read Carefully):**
You are working inside `midi-web-apps-portal`, a monolithic React/Vite application that acts as a master DAW harness for several "headless" MIDI plugins.

* The architecture is strictly divided into `src/core/` (the master harness) and `src/plugins/` (the child MIDI processors).
* Currently, the Portal has a placeholder "Zone D" at the bottom of the screen (`src/core/App.jsx`) meant for a master audio engine.

**The Objective:**
Replace the placeholder "Zone D" drawer with the fully functional, fully styled Rompler audio engine from the original `midi-chord-notator-web` repository. You must fetch the untouched source code directly from GitHub, silo it within our core architecture, and wire it up.

**Execution Plan: Strict Isolation & Remote Fetching**
Execute these steps sequentially.

**Step 1: Fetching the Source of Truth**
Because our local copies of the child apps have been gutted, you must fetch the original Rompler code from the remote repository.

1. Clone the repository into a temporary directory: `git clone https://github.com/craig-van-hise/midi-chord-notator-web.git temp-repo`
2. Identify the Rompler UI components, Tone.js audio engine logic, and related assets inside `temp-repo/src/`.

**Step 2: Establishing the Core Silo**

1. Create a dedicated silo for the audio engine in the portal: `mkdir -p src/core/rompler`
2. Move the relevant Rompler components (the Drawer, the Tab, the internal layout, and the Tone.js engine wrapper) from `temp-repo` into `src/core/rompler/`.
3. Clean up: `rm -rf temp-repo`

**Step 3: Strict CSS & Font Migration**
We must perfectly preserve the original dark-mode, hardware-esque aesthetic of the Rompler (the specific green accents, digital fonts, and exact knob/slider layouts).

1. Isolate the CSS: Create `src/core/rompler/rompler.css`. Extract ONLY the CSS rules that applied to the Rompler from the original codebase and put them here.
2. Ensure any custom fonts (like the digital LED font for the MIDI channel/Instrument patch) are properly imported into the portal's `public/fonts` directory and referenced correctly in `rompler.css`.
3. Wrap the root component of the Rompler in a specific class (e.g., `.master-rompler-wrapper`) to ensure global Tailwind styles do not corrupt its native layout.

**Step 4: Wiring the Harness**

1. Open `src/core/App.jsx`. Locate the placeholder "Zone D | MASTER ROMPLER AUDIO ENGINE".
2. Replace this placeholder with the newly imported `<MasterRompler />` component.
3. **Audio Routing:** The Portal receives processed MIDI data from the active plugin via an `onMidiOut` callback. You must pipe this MIDI data directly into the `<MasterRompler />`'s Tone.js engine so it triggers the corresponding notes/samples.
4. **AudioContext Unlock:** Retain the "Click to Start Audio Engine" overlay logic currently in the portal to ensure browser autoplay policies are respected before Tone.js initializes.

**HARD STOP: Human Validation Checkpoint**
Once the Rompler is ported, styled, and wired, halt execution. Output the following exact text:

"**MASTER ROMPLER INTEGRATED. READY FOR FINAL VALIDATION.** Please run `npm run dev` and test the following:

1. Look at the bottom drawer tab. Verify it matches the original 'ROMPLER' styling with the green status dot.
2. Open the drawer. Verify the exact UI (digital fonts, knobs, instrument dropdown) has been preserved and isn't distorted by Tailwind.
3. Click 'Start Audio Engine', play a MIDI note through one of the plugins, and verify the Rompler produces sound.

If everything works perfectly, reply **'VALIDATED'** and the audio integration is complete."