

> **DO NOT VALIDATE YET. ROMPLER ANIMATION UX FIX**
> **Context:** The Rompler drawer works, but the open/close animation is currently using a strange, bouncy, sluggish physics curve. It snaps fast and then bounces at the end. This is bad UX for a DAW interface.
> **The Objective:** Remove the bouncy physics/spring animation completely and replace it with a clean, professional, linear or standard ease-in-out slide.
> **Step 1: Locate the Animation Logic**
> Look at the root wrapper of the `<MasterRompler />` component (likely in `src/core/rompler/index.jsx` or similar).
> **Step 2: Strip the Physics**
> * **If using Framer Motion (`<motion.div>`):** Change the `transition` prop. Remove `type: "spring"`, `bounce`, `stiffness`, or `damping`. Replace it with a simple tween: `transition={{ type: "tween", duration: 0.3, ease: "easeInOut" }}`.
> * **If using standard Tailwind:** Remove any custom Bezier curves or spring utility classes. Ensure the wrapper only uses `transition-transform duration-300 ease-in-out`.
> 
> 
> **Step 3: Verify the State Transforms**
> Ensure the open state maps strictly to `translate-y-0` and the closed state maps strictly to `translate-y-full` (minus whatever offset is needed to keep the Tab visible).
> **Execution:** Strip out the physics animation, apply the standard 0.3s ease-in-out slide, and ask me to visually confirm the smooth movement.