
**STOP. You are violating the core architecture of this project.** Look at the `README.md`. The Rompler and all AudioContext/Tone.js logic belong **EXCLUSIVELY** to the Master Portal Host (`src/core/rompler`). Individual plugins (like `chord-notator`) are strictly HEADLESS. They do not generate audio. They do not touch Tone.js. They only process MIDI data.

Execute the following fixes immediately:

1. **Rip Out Plugin Audio Logic:** Go into `src/plugins/chord-notator/` and completely remove ANY audio initialization, Tone.js imports, or `engine.ts` files you were messing with. The plugin must revert to purely handling React UI and emitting `onMidiOut` events.
2. **Centralize the Rompler:** Move all low-latency `AudioContext` creation and `bootEngine()` logic back strictly into `src/core/rompler/engine.js` and `src/core/rompler/MasterRompler.jsx`.
3. **Acknowledge the Architecture:** Before you write another line of code, confirm that you understand the plugins are headless and the Rompler is a global portal asset.
4. **Fix the Audio:** Implement the deferred `bootEngine()` fix in the Master Rompler exactly as previously instructed, without touching the plugins.

Do not mark anything as completed until I manually verify the Master Rompler is generating sound.