I have Git merge conflicts breaking my Vite build. The conflict markers (`<<<<<<< HEAD`) are causing PARSE_ERRORs in `src/core/App.jsx` and `src/core/rompler/engine.js`. Please resolve all merge conflicts in this project by accepting the **Current Changes (HEAD)**, as those contain my latest saved work. Strip out all Git conflict markers, ensure the files have valid syntax so Vite stops crashing, and stage the resolved files."

