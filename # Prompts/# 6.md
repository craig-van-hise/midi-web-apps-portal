
> **DO NOT VALIDATE YET. CLEF VERTICAL ALIGNMENT BUG**
> **Context:** The previous CSS fix worked to restore the staff lines and general rendering, but there is a specific vertical alignment (y-axis offset) bug. Both the Treble and Bass clef glyphs are rendering approximately two "staff spaces" too low relative to the 5-line staff.
> **The Objective:** Fix the vertical alignment of the clefs so they sit properly on their respective staves.
> **Step 1: Check for CSS Inheritance Conflicts**
> 1. The `midi-web-apps-portal` uses Tailwind CSS, which applies a global reset and base styles (like a default `line-height: 1.5` or `1.6`).
> 2. Inspect the container `<div>` inside `src/plugins/chord-notator/index.jsx` that holds the notation renderer.
> 3. Apply defensive CSS to this specific container to prevent Tailwind from disrupting font-based rendering. Add classes like `leading-none` (line-height: 1) or explicitly set the style `lineHeight: "1"` on the wrapper.
> 
> 
> **Step 2: Inspect the Renderer Logic (VexFlow / SMuFL / Custom)**
> 1. Look into the specific code that draws the staff and the clefs (likely in the `components` or `utils` folder of the ported plugin).
> 2. If the application uses a library like VexFlow: Ensure the bounding box or font scaling wasn't accidentally altered.
> 3. If the application draws raw SVGs: Check the `<g>` or `<text>` tags rendering the clefs. Look for a `y` attribute or a `transform="translate(x, y)"` attribute. You need to decrease the `y` value (move it UP) to compensate for the offset.
> 
> 
> **Execution:** > 1. Apply the defensive `line-height` and typography resets to the notation wrapper container.
> 2. If the CSS reset doesn't fix it, manually adjust the `y` offset parameters in the drawing logic to shift the clefs UP by approximately two staff spaces.
> 3. Run the dev server, verify the fix visually, and report back with exactly what code you changed.