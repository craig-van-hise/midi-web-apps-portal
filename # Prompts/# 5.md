

> **DO NOT VALIDATE YET. UI REGRESSION ON PLUGIN #1**
> **Context:** The JavaScript logic, porting, and prop-passing for `ChordNotator` are working perfectly. However, the visual rendering of the sheet music is completely broken. The grand staff bracket is missing, the bass clef is missing, and the notes/accidentals are floating unaligned in the whitespace.
> **The Objective:** Identify and migrate the missing global CSS, font declarations, or container styling that the notation renderer relies on.
> **Step 1: Audit Global CSS & Fonts**
> 1. Read `../midi-chord-notator-web/src/index.css` and `../midi-chord-notator-web/src/App.css`.
> 2. Look for any custom CSS classes (especially those related to `.staff`, `.notation`, `.vexflow`, or SVG elements) that were NOT migrated to `midi-web-apps-portal/src/core/index.css`.
> 3. Look for any `@font-face` declarations. You previously copied `public/fonts`, but if the portal's CSS doesn't have the `@font-face` rules to load them, the notation characters (like clefs and accidentals) will fail to render or align correctly.
> 4. Migrate any missing global CSS and `@font-face` rules into the Portal's main CSS file.
> 
> 
> **Step 2: Audit the Container Wrapper**
> 1. Compare the new `src/plugins/chord-notator/index.jsx` wrapper you created against the original `../midi-chord-notator-web/src/App.jsx`.
> 2. Did you accidentally strip out a crucial layout `<div>` (e.g., a `relative`, `flex`, `w-full`, or specific `min-height` container) that the notation component relies on to calculate its SVG/Canvas dimensions?
> 3. Restore the exact DOM container hierarchy around the notation component that existed in the standalone app.
> 
> 
> **Execution:** Investigate the original CSS and App.jsx wrapper, migrate the missing styles/fonts to the portal, fix the DOM hierarchy in `index.jsx`, and run tests. Output a summary of the specific missing CSS/fonts you migrated and ask me to check the UI again.