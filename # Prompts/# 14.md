

> **DO NOT VALIDATE YET. FINAL UI POLISH & LAYOUT SHIFTS**
> **Context:** The previous changes worked structurally, but there are four specific UI/UX details that are broken. We need to fix the Rompler's closed state, fix the missing font, set the default state, and fix the jumping sidebar toggle button.
> **Issue 1: Rompler Tab is Stifled by a Black Bar**
> When the Rompler is closed, there is a full-width black bar at the bottom of the screen. It should ONLY be the small "ROMPLER" tab sticking up from the bottom boundary.
> * **Fix:** Inspect the `<MasterRompler>` wrapper. When `isOpen` is false, the parent container should NOT have a dark background color stretching across the bottom. The drawer body should translate completely off-screen (`translate-y-full`), and the tab should use absolute positioning (e.g., `-top-10`) with `overflow-visible` on the container so *only* the tab peeks out over the main content area.
> 
> 
> **Issue 2: The LED Font is Still Missing**
> The green "01" and "VIBRAPHONE" text is still using standard sans-serif instead of the digital/LED font.
> * **Fix:** Check `public/fonts/`. What is the exact filename of the LED font? Go to `src/core/rompler/rompler.css` and verify the `@font-face` declaration uses an absolute path like `src: url('/fonts/FileName.ttf')`. Ensure the CSS class applying this `font-family` is actually attached to the `<div>` elements rendering the text.
> 
> 
> **Issue 3: Drawer Default State**
> The Rompler is defaulting to open.
> * **Fix:** Change the `useState` initialization for the Rompler's open state to `false`.
> 
> 
> **Issue 4: Sidebar Toggle Button Jumps (UX Bug)**
> When collapsing the sidebar, the toggle button jumps to a floating position lower on the screen. To maintain user muscle memory, the toggle button MUST remain at the exact same vertical (Y) coordinate whether the sidebar is open or closed so the user doesn't have to chase it with their mouse.
> * **Fix:** Do not render a completely different floating button when collapsed. Keep the sidebar container, but change its width from `w-64` to `w-16` (or similar). Keep the top header area intact. When collapsed, hide the "MIDI MODULES" text, but keep the `<` / `>` toggle button exactly where it is vertically (e.g., in a fixed `h-14` header bar).
> 
> 
> **Execution:** Apply these 4 surgical fixes. Run the dev server. Verify the tab has no black bar next to it, the font is digital, the drawer starts closed, and the sidebar button stays vertically locked when clicked. Report back for validation.